{
  "name": "CryptoPrices",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "rm -rf dist && parcel build index.html --public-url minimalist-crypto-prices",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "js": "^0.1.0"
  },
  "devDependencies": {
    "cssnano": "^4.1.7",
    "gh-pages": "^2.0.1"
  }
}
